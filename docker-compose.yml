services:
  qc-postgres:
    image: postgres:16
    container_name: qc-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: qc
      POSTGRES_PASSWORD: qc
      POSTGRES_DB: qc
    volumes:
      - qc_postgres_data:/var/lib/postgresql/data

volumes:
  qc_postgres_data:

